import React from "react";
import { Page, Header, Body } from "../../../layout";

const Loading = () => (
    <Page>
        <Header>Loading...</Header>
        <Body>
            <p>We are loading your code-splitted content now...</p>
        </Body>
    </Page>
);

export default Loading;
